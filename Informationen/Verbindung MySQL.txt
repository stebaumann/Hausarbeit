import sqlalchemy as db
import os 


#Bibliothekebversionen anzeigen
print ('SQLAlchemy-Version:', db.__version__)

#ein Engine-Objekt erzeugen
hostname = "localhost"
username = "root"
password = "MasterInformatik"
port = 3306
database = "movie"
#my_password = os.getenv("MasterInformatik")
#print(my_password)
#con_str = "mysql+pymysql://root:" +"MasterInformatik" + "@localhost:3306/movie"
con_str = "mysql+pymysql://" +username+":"+password+"@"+ hostname+":"+str(port)+"/"+database
print (con_str)
engine = db.create_engine(con_str)

#ein DB-Verbindugsobjekt erzeugen

connection = engine.connect()